实验名称：CPU调度算法模拟实现
1、SJF（非抢占）
1）输入各进程运行时间（以空格间隔），可手动输入也可文件读取。
2）结果输出各进程的开始时间和结束时间、进程的平均等待时间，和具体的甘特图。
2、SJF（抢占）
1）输入各进程到达时间（以空格间隔）和运行时间（以空格间隔），可手动输入也可文件读取。
2）结果输出各进程的开始时间和结束时间、进程的平均等待时间，和具体的甘特图。
3、RR
1）输入时间片和各进程运行时间（以空格间隔），可手动输入也可文件读取。
2）结果输出各进程的开始时间和结束时间、进程的平均等待时间，和具体的甘特图。